
import { logger } from "."
import { TEXT } from "../utils/colors"

logger(__filename, "   The getUserMedia API")

console.log(`${TEXT.BOLD}${TEXT.YELLOW}Media Stream (getUserMedia)${TEXT.CLOSURE}`)

console.log("\nAccesses the user's camera and microphone")

console.log("Check out '02.getUserMedia' folder to explore the code (.js)")
